- [x] Inspect repo structure and locate image assets.
- [x] Inspect Streamlit `app.py` image usage.
- [ ] Patch Streamlit `app.py` to use absolute asset paths via `Path(__file__)` so images always load.
- [ ] Add missing `footwear` mapping to `ASSETS`.
- [ ] Re-run `streamlit run app.py` and verify images render.

